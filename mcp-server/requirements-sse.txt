# Requirements for MCP SSE Server
# Online HTTP/SSE transport version

# Core dependencies
mcp>=1.0.0
httpx>=0.25.0
pydantic>=2.0.0

# FastAPI for HTTP/SSE server
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# Development dependencies (optional)
pytest>=7.0.0
pytest-asyncio>=0.21.0
